import "package:flutter/material.dart";
import 'package:moment/constants/app.dart';

// todo
class HomeBody extends StatelessWidget {
  Map moment;

  HomeBody({this.moment});

  @override
  Widget build(BuildContext context) {
    return Text(moment['']);
  }
}
